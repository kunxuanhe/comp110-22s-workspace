"""Some helpdul utility functions for working with CSV files."""

def read_csv_rows(filename: str) -> list[dicr[str, str]]:
    """Read the rows of a csv into a 'table'."""
    result: list[dict[str, str]] = []
    # TODO: More work!
    return result